import 'package:flutter/material.dart';

class SpeechSettingScreen extends StatelessWidget {
  const SpeechSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Speech Setting Screen')));
  }
}
