<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- 배경이 먼저 선언 -->
    <ImageView
        android:id="@+id/floating_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/floating_bar"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:padding="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 버튼 그룹이 나중에 선언(항상 위에 보임) -->
    <LinearLayout
        android:id="@+id/button_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:clipChildren="false"
        android:clipToPadding="false"
        app:layout_constraintTop_toTopOf="@id/floating_bar"
        app:layout_constraintBottom_toBottomOf="@id/floating_bar"
        app:layout_constraintStart_toStartOf="@id/floating_bar"
        app:layout_constraintEnd_toEndOf="@id/floating_bar">

        <ImageButton
            android:id="@+id/btn_return"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@null"
            android:src="@drawable/ic_return"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="0dp"
            android:padding="8dp"/>

        <View
            android:id="@+id/space"
            android:layout_width="0dp"
            android:layout_height="37dp" />

        <ImageButton
            android:id="@+id/btn_close"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@null"
            android:src="@drawable/ic_close"
            android:layout_marginBottom="16dp"
            android:padding="8dp"/>
    </LinearLayout>

    <!-- 임시 신고 버튼 (배경 아래) -->
    <ImageButton
        android:id="@+id/btn_report"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@null"
        android:src="@drawable/icon_report1"
        app:layout_constraintTop_toBottomOf="@id/floating_bar"
        app:layout_constraintStart_toStartOf="@id/floating_bar"
        app:layout_constraintEnd_toEndOf="@id/floating_bar"
        android:layout_marginTop="8dp"
        android:padding="8dp"/>
</androidx.constraintlayout.widget.ConstraintLayout> 